<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IStrategyDao">

    <resultMap id="DataMap" type="cn.bugstack.infrastructure.dao.po.Strategy">
        <id column="id" property="id"/>
        <id column="strategy_id" property="strategyId"/>
        <id column="strategy_desc" property="strategyDesc"/>
        <id column="rule_models" property="ruleModels"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryStrategyList" resultMap="DataMap">
        select strategy_id, strategy_desc, rule_models
        from strategy
    </select>

    <select id="queryStrategyEntityByStrategyId" parameterType="java.lang.Long" resultMap="DataMap">
        select strategy_id, strategy_desc, rule_models
        from strategy
        where strategy_id = #{strategyId}
    </select>
</mapper>
